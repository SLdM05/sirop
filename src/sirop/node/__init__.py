"""Bitcoin node verification and on-chain graph traversal.

Optional enrichment layer — the pipeline must function identically
(minus graph-assisted transfer matching) when this module is unavailable
or ``BTC_TRAVERSAL_MAX_HOPS=0``.
"""
